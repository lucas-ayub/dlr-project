# pyproject.toml (raiz do repo)
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sar-recon"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["numpy", "matplotlib"]

[tool.setuptools.packages.find]
where = ["sar_reconstruction"]

